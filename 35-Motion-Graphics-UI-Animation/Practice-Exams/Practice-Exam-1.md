---
permalink: /35-Motion-Graphics-UI-Animation/Practice-Exams/Practice-Exam-1/
title: "Practice Exam 1: Motion Design Foundations (Modules 1–5)"
---

# 🧪 Practice Exam 1 — Motion Design Foundations

**Covers:** Modules 1–5 (Principles, Expressions, Typography, Logo Motion, Infographics)  
**Time:** 40 minutes  
**Questions:** 30  
**Passing Score:** 75% (23/30)

---

### 1.
Which of Disney's 12 principles describes an object elongating in the direction of travel while preserving its volume?

A. Anticipation  
B. Squash and Stretch  
C. Exaggeration  
D. Follow-Through  

---

### 2.
The CSS function `cubic-bezier(0, 0, 0.58, 1)` corresponds to which named easing?

A. ease-in  
B. ease  
C. ease-out  
D. linear  

---

### 3.
A motion designer applies `loopOut("offset")` to a Y position property that animates from 0px to 80px. After two loops, the position value is:

A. 0px  
B. 80px  
C. 160px  
D. -80px  

---

### 4.
In the Range Selector for a text reveal, the `Offset` property is keyframed from -100% to 100%. The `Based On` setting is `Words`. The result is:

A. Each character reveals from left to right  
B. Each word reveals from left to right as a unit  
C. The entire text block fades in at once  
D. Lines reveal from top to bottom  

---

### 5.
The Blur > Scale > Opacity text reveal is characterized by which starting state of Scale (in the "hidden" position)?

A. 50% — characters start smaller and grow to full size  
B. 100% — characters start at normal size  
C. 120% or similar — characters start slightly oversized and deflate to final size  
D. 0% — characters scale from invisible  

---

### 6.
When should `ease-in` be applied to an element's animation?

A. When the element enters the screen  
B. When the element moves across the screen  
C. When the element exits the screen  
D. Always — ease-in is the default AE easing  

---

### 7.
The expression `wiggle(5, 15, 1, 0.5, time + index * 50)` applied to multiple layers creates:

A. All layers wiggle identically at 5Hz with 15px amplitude  
B. Each layer wiggles at a different phase offset, creating de-synced unique wiggle per layer  
C. Layers wiggle with increasing amplitude based on index  
D. Each layer's wiggle frequency increases by 50Hz  

---

### 8.
`valueAtTime(time - 0.25)` applied to Layer B's Position, referencing Layer A, creates:

A. Layer B is 25% smaller than Layer A  
B. Layer B mirrors Layer A's position but lags 0.25 seconds behind  
C. Layer B moves to where Layer A will be in 0.25 seconds  
D. Layer B's position is offset by 25 pixels  

---

### 9.
Which AE Trim Paths property, when animated from 0% to 100%, creates the draw-on logo reveal effect?

A. Start  
B. End  
C. Offset  
D. Both Start and End simultaneously  

---

### 10.
For shape layer morphing in After Effects, the most critical technical requirement is:

A. Both shapes must be on the same layer  
B. Both shapes must have identical colors  
C. Both shapes must have the same number of anchor points  
D. Both shapes must be converted to raster before morphing  

---

### 11.
Netflix's N ident uses which primary reveal technique?

A. Draw-on trace along the N paths  
B. Shape morph from a circle to the N letterform  
C. Z-axis layered depth reveal — strips separate in 3D space before collapsing to 2D  
D. Classic 3D Y-axis flip  

---

### 12.
Broadcast legal luminance levels in HD video restrict values to which range?

A. 0–255 (full range)  
B. 0–235  
C. 16–235  
D. 32–220  

---

### 13.
The After Effects compositions intended for broadcast HD delivery should use which color profile?

A. sRGB  
B. Adobe RGB  
C. Rec. 709 (BT.709)  
D. Display P3  

---

### 14.
The ideal bar-to-gap ratio for bar charts is:

A. 1:1  
B. 2:1 (bar twice as wide as gap)  
C. 1:2  
D. 3:2  

---

### 15.
Which AE chart animation technique is used to create an animated line chart that progressively reveals left to right?

A. Mask wipe animated across the layer  
B. Scale X animated from 0% to 100%  
C. Trim Paths End animated from 0% to 100%  
D. Stroke Effect with animated End property  

---

### 16.
AEJuice presets used in a client project should always be:

A. Used as-is — they are designed for professional delivery  
B. Customized (timing, color, typography) before delivery; unmodified presets are immediately recognizable  
C. Only used if the client owns an AEJuice license  
D. Limited to 5 presets per project  

---

### 17.
The data storytelling principle states the key insight should appear at approximately what point in an animation?

A. At the start  
B. At the 50% mark  
C. At the 2/3 mark  
D. At the very end  

---

### 18.
The expression `ease(time, 0, 2, 0, 100)` applied to Opacity produces:

A. Linear fade from 0 to 100 over 2 seconds  
B. Smooth (eased) fade from 0 to 100 over the first 2 seconds, then holds at 100  
C. 2-second delay before opacity begins  
D. Opacity oscillates between 0 and 100 at 2 Hz  

---

### 19.
Disney's principle of "Staging" in motion graphics means:

A. Using a three-point lighting setup  
B. Only one primary motion event at a time, so the viewer's eye always has a clear focus  
C. Organizing compositions into pre-comps  
D. Compositing motion elements over a static background  

---

### 20.
In motion design, what does the `index` expression property return?

A. The layer's Z-depth value  
B. The layer's position in the layer stack (1 = topmost)  
C. The number of frames in the composition  
D. The current time in frames  

---

### 21.
The "Tone of Motion" concept states that:

A. Animation speed must equal the musical BPM divided by 60  
B. The emotional quality of audio and animation should match (or deliberately contrast)  
C. All animations should have the same tone of easing curve  
D. Color temperature of motion elements should match the music  

---

### 22.
What is the correct syntax for referencing a Slider Control named "Speed" on a null layer named "CTRL"?

A. `effect("CTRL")("Speed")`  
B. `thisComp.layer("CTRL").effect("Speed")("Slider")`  
C. `master.speed`  
D. `CTRL.effect["Speed"].value`  

---

### 23.
For broadcast package design, what are the standard safe zones?

A. Title Safe: 80%, Action Safe: 93.75%  
B. Title Safe: 90%, Action Safe: 95%  
C. Title Safe: 75%, Action Safe: 88%  
D. Title Safe: 85%, Action Safe: 93.75%  

---

### 24.
The `loopOut("continue")` expression:

A. Loops keyframes by repeating them cyclically  
B. Plays keyframes forward then backward  
C. Continues the velocity of the last keyframe indefinitely (no loop — continuous drift)  
D. Continues playing the first keyframe value forever  

---

### 25.
Google's animated logo always presents its four colors in which order?

A. Red, Blue, Yellow, Green  
B. Blue, Red, Yellow, Green  
C. Blue, Green, Yellow, Red  
D. Green, Yellow, Red, Blue  

---

### 26.
When importing a vector logo from Illustrator for animation, which step makes each element independently animatable?

A. Import as footage using "Merged Layers"  
B. Layer > Create > Create Shapes from Vector Layer  
C. Import as composition maintaining layer sizes  
D. Convert to raster in Illustrator before import  

---

### 27.
The Range Selector "Ease High" parameter controls:

A. The easing of keyframes on the Offset property  
B. The transition softness at the high end of the affected range  
C. The maximum opacity characters reach  
D. How fast the text reveals at the end of the animation  

---

### 28.
Disney's principle of "Follow-Through and Overlapping Action" in motion graphics is best demonstrated by:

A. All elements animating simultaneously with the same duration  
B. Staggered exits where secondary elements trail after the primary element stops  
C. Anticipatory motion before the main action  
D. Scaling an element slightly beyond 100% before settling  

---

### 29.
A waffle chart built in After Effects uses 100 cells (10×10 grid) to represent percentages. What expression property makes each cell unique in its animation timing?

A. `time`  
B. `random()`  
C. `index`  
D. `thisComp.duration`  

---

### 30.
Which statement about "royalty-free" music is correct?

A. Royalty-free music is always free to use commercially  
B. Royalty-free music requires no credit in video descriptions  
C. Royalty-free means you pay once and don't owe per-use royalties — but a license fee is required and terms must be followed  
D. Royalty-free music can be re-sold to third parties  

---

## 🎯 Answer Key (No Cheating!)

```
1:  B — Squash and Stretch. Objects elongate in motion direction (stretch) and compress on impact (squash). Volume is preserved.

2:  C — cubic-bezier(0, 0, 0.58, 1) = ease-out. Fast start, slow finish.

3:  C — loopOut("offset") adds the delta (80px) each loop. After 2 loops: 160px. It drifts infinitely.

4:  B — Based On: Words causes each word to animate as a unit. The Offset sweep still goes left to right, but reveals whole words at a time.

5:  C — 120% (or similar value above 100%). Characters start oversized and "deflate" to final size — the impression of arriving from "above."

6:  C — ease-in for exits: elements accelerate as they leave the screen, matching the physics of something departing.

7:  B — The `time + index * 50` parameter de-syncs each layer's wiggle by offsetting the noise sample time. Each layer produces a unique wiggle pattern.

8:  B — Layer B mirrors Layer A's position with a 0.25-second delay (lag). Not future-looking — it samples the past.

9:  B — Trim Paths End: 0% = nothing visible, 100% = full path visible. Animating End from 0 to 100 draws the path on progressively.

10: C — Same number of anchor points. AE maps Point 1 to Point 1. Mismatched counts cause tearing during morph.

11: C — The N ident uses Z-axis depth layers that separate in 3D before snapping flat. Not a draw-on or a Y-axis flip.

12: C — Broadcast legal: 16–235 luminance range (0–255 scale). Values outside this can cause signal issues in broadcast transmission.

13: C — Rec. 709 (BT.709) is the HD broadcast color standard. sRGB is for web; Adobe RGB is for print/photography.

14: B — 2:1 ratio (bar twice as wide as gap). Standard data visualization best practice for bar charts.

15: C — Trim Paths End: 0% → 100% progressively reveals the line path from start to end — the line "draws" across the chart.

16: B — Always customize AEJuice presets. Unmodified presets are immediately identifiable to experienced viewers.

17: C — 2/3 mark. First 1/3 = context; middle 1/3 = data change; 2/3 mark = insight reveal; final 1/3 = absorption time.

18: B — ease() creates smooth (ease-in-out) transition from 0 to 100 over the first 2 seconds, then holds at 100.

19: B — Staging: one primary motion at a time. The viewer's eye needs a single focus at every moment.

20: B — index returns the layer's position in the layer stack. 1 = topmost layer. Used for per-layer offsets.

21: B — Tone of Motion: audio emotional quality should match (or deliberately contrast) animation vocabulary.

22: B — Correct syntax: thisComp.layer("CTRL").effect("Speed")("Slider")

23: A — Title Safe: 80%, Action Safe: 93.75%. These are the broadcast standard zone percentages.

24: C — loopOut("continue") continues the velocity of the last keyframe forever — it doesn't repeat, it continues. Creates infinite drift in one direction.

25: B — Google's canonical order: Blue, Red, Yellow, Green.

26: B — Layer > Create > Create Shapes from Vector Layer. This converts Illustrator artwork to editable AE shape layers.

27: B — Ease High controls the softness of the transition at the high end of the Range Selector's affected range.

28: B — Follow-Through and Overlapping Action: parts stop at different times. Staggered exits are the motion graphics translation.

29: C — index. Each cell has a unique index (1–100) which is used to calculate its unique animation delay.

30: C — Royalty-free: one-time fee, no per-use royalties. Not free to use. Terms must still be read and followed.
```
