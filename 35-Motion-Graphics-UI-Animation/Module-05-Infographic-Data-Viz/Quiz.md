---
permalink: /35-Motion-Graphics-UI-Animation/Module-05-Infographic-Data-Viz/Quiz/
title: "Module 5 Quiz: Infographic & Data Viz"
---

# 🧠 Module 5 Quiz — Infographic & Data Viz

### Q1.
When animating a bar chart in After Effects, the anchor point of each bar should be positioned at:

A. The top center, so bars scale down from the top  
B. The center, so bars scale from the middle  
C. The bottom center, so bars scale upward from the baseline  
D. The left center, so bars wipe in from left  

---

### Q2.
The "draw-on" reveal for a line chart in After Effects is achieved using:

A. The Stroke effect with animated length  
B. Trim Paths End property animated from 0% to 100%  
C. The Write-On effect applied to the line layer  
D. A mask wipe animated across the layer  

---

### Q3.
Which technique is typically used to create a donut chart (pie chart with a hole in the center) in After Effects?

A. Apply the Polar Coordinates effect to a bar layer  
B. Use a circular Track Matte to cut out the center of a solid pie chart  
C. Use the Doughnut shape tool built into AE's shape layer options  
D. Import from Adobe Illustrator with the hole pre-cut  

---

### Q4.
In a waffle chart built with index expressions, 100 cells represent 100%. If you want to show 73%, how many cells should animate to their "filled" state?

A. 7  
B. 37  
C. 73  
D. 100  

---

### Q5.
Which data storytelling principle states that the key insight should appear at approximately what point in the animation's timeline?

A. At the start — front-load the insight  
B. At the 50% mark — the middle is most memorable  
C. At the 2/3 mark — after establishing context  
D. At the very end — save the best for last  

---

### Q6.
Motion Bro is a tool for:

A. Exporting After Effects animations to web formats  
B. Managing and previewing third-party preset packs within the AE interface  
C. Importing motion capture data into AE  
D. Generating automated animation rigs for character animation  

---

### Q7.
When using AEJuice presets on a client project, the professional approach is:

A. Use presets as-is — they are designed for professional use  
B. Only use presets from the paid AEJuice packs, not the free ones  
C. Always customize timing, color, and typography before delivery — unmodified presets are immediately recognizable  
D. Get written permission from AEJuice before using in commercial projects  

---

### Q8.
A choropleth map animation uses which technique to visualize regional data?

A. Animated route lines tracing between regions  
B. Fill color encoding per region, animated as data changes  
C. Proportional circles (proportional symbol map)  
D. 3D extrusion height per region  

---

### Q9.
The ideal bar-to-gap ratio in bar chart design is:

A. 1:1 (bar width equals gap width)  
B. 1:2 (bar half as wide as gap)  
C. 2:1 (bar twice as wide as gap)  
D. 3:1 (bar three times wider than gap)  

---

### Q10.
In a data animation, when should annotation text labels appear relative to the data event they describe?

A. Before the data event, to prime the viewer  
B. At the same time as the data event — synchronized precisely  
C. After the data event, as a summary  
D. At a fixed time (e.g., always at 50% of total duration)  

---

### Q11.
Hans Rosling's animated bubble chart presentations (Gapminder) are historically significant in data animation because:

A. They introduced the pie chart as an animated format  
B. They demonstrated that animation can make data emotionally compelling and reveal causality in time-series data  
C. They were the first to use After Effects for data visualization  
D. They established the 60-second duration as the standard for data animations  

---

### Q12.
To make chart bar labels (text layers) that follow the top of each animated bar, you would:

A. Manually keyframe each text layer's position to match bar heights  
B. Link the text layer's Y position to the bar's Scale Y via expression  
C. Use a Motion Sketch to record the bar's motion and apply it to the text  
D. Parent the text layer to the bar layer  

---

### Q13.
Which of the following violates the "Animate What Carries the Data" principle?

A. Animating a bar's height to represent a numeric value  
B. Animating the chart frame's border with a spinning glow effect  
C. Animating a line chart to reveal left to right  
D. Animating a map route to trace a travel path  

---

### Q14.
In a news graphics context, a "Trim Paths" reveal on a map route layer communicates:

A. The geographic borders of regions  
B. A path being traveled, drawn, or traced in real-time — a journey or route  
C. Color coding by region  
D. A zoom animation from a wide view to a close-up  

---

### Q15.
When building an animated infographic template that multiple team members will update with new data, the best approach is:

A. Export as a flat MP4 and rebuild from scratch for each new dataset  
B. Build a Master Controller system where one slider or set of sliders drives all chart values via expressions  
C. Use a separate AEP file for each version of the data  
D. Use After Effects' native CSV import (which does not exist)  

---

### Q16.
The principle "proportional duration" in data animation states:

A. Chart elements should always be proportional to the screen height  
B. The animation duration of a value change should be proportional to the magnitude of the change  
C. All chart bars should animate in exactly the same number of frames  
D. The total animation duration should be proportional to the dataset size  

---

### Q17.
Color in an animated infographic should primarily be used to:

A. Make the animation more visually interesting with multiple hues  
B. Encode variables and carry meaning (e.g., profit = green, loss = red)  
C. Match the client's brand colors on all elements including axes and labels  
D. Distinguish between animated and static elements  

---

### Q18.
For a staggered bar chart entrance where each bar starts its animation 5 frames after the previous one (at 24fps), the delay in seconds for bar #4 is:

A. 0.2 seconds  
B. 0.42 seconds  
C. 0.5 seconds  
D. 0.625 seconds  

---

### Q19.
A motion designer is building an animated map that zooms from a world view into a specific country. Which AE technique creates this effect most naturally?

A. Scale and Position keyframes on the map layer  
B. Camera Z-position animation with a map layer at a fixed Z-depth  
C. A Warp effect that distorts the map to the region  
D. The Magnify effect on the target region  

---

### Q20.
AEJuice's free "Starter Pack" primarily offers which type of content?

A. 3D rendering templates  
B. Sound effects and music  
C. Transitions, text presets, and infographic elements  
D. Video footage and stock photography  

---

### Q21.
Which animation principle is most directly violated when multiple chart variables change simultaneously with no visual hierarchy?

A. Squash and Stretch  
B. Staging (one primary motion at a time; the eye must know where to look)  
C. Follow-Through  
D. Exaggeration  

---

### Q22.
When should a bar chart be preferred over a pie chart for data communication?

A. When showing parts of a whole that add up to 100%  
B. When comparing values across categories, especially more than 5 categories  
C. When showing change over time  
D. When the data has a circular shape  

---

### Q23.
In a Trim Paths line chart reveal, adding a small dot element that travels along the line as it draws creates which effect?

A. A waypoint marker at the end of the line  
B. A leading indicator that gives the eye something to follow — the dot "pulls" the line forward  
C. A pulsing animation at key data points  
D. A shadow following the line path  

---

### Q24.
After Effects does NOT natively import which of the following data formats directly?

A. CSV files as editable data  
B. Illustrator SVG files  
C. Photoshop PSD files  
D. Premiere Pro sequences via Dynamic Link  

---

## 🎯 Answer Key (No Cheating!)

```
Q1:  C — Bottom center anchor so Scale Y animation grows the bar upward from the baseline. Top-center anchor would grow downward.

Q2:  B — Trim Paths End property animates from 0% (no line visible) to 100% (full line visible), creating the progressive draw-on reveal.

Q3:  B — A Track Matte (specifically an Alpha Matte from a solid circle shape layer) masks the center of the pie, creating the donut cutout.

Q4:  C — 73 cells fill for 73%. The waffle chart maps directly: 1 cell = 1%.

Q5:  C — The insight should appear at the 2/3 mark. The first third establishes context (what are we measuring?); the last third gives the viewer time to absorb the insight.

Q6:  B — Motion Bro is a panel extension for managing and previewing preset packs within AE. It doesn't export or import motion data.

Q7:  C — Unmodified AEJuice presets are immediately identifiable by experienced designers. Always customize timing, color, and typography for professional delivery.

Q8:  B — A choropleth map encodes data as fill color per region. Higher values = darker/more saturated color (or the variable's chosen color scale).

Q9:  C — 2:1 ratio (bar twice as wide as gap) is the standard data visualization guideline. Gaps wider than bars make the chart feel sparse; equal widths feel mechanical.

Q10: B — Annotations should be precisely synchronized to the data event they describe. Pre-display is confusing (what does this label refer to?); post-display means the viewer missed the connection.

Q11: B — Rosling's work demonstrated that animation can make time-series data emotionally compelling and reveal causal relationships (e.g., wealth driving health) that static charts cannot convey.

Q12: B — Link the text layer's Y Position to the bar's Scale Y value via an expression. When the bar reaches 70% height, the label tracks to 70% height position.

Q13: B — Animating the chart frame's border with a decorative glow effect is motion that doesn't carry data — it's decorative noise. The other options all animate elements that represent the data.

Q14: B — Trim Paths on a route line creates a progressive draw that reads as a journey or path being traced — the natural visual metaphor for travel/routing.

Q15: B — A Master Controller with sliders for each data value, linked via expressions to chart elements, allows team members to update values without touching keyframes.

Q16: B — Proportional duration: a 200% increase should animate roughly twice as long as a 100% increase, preserving the viewer's intuitive sense of scale.

Q17: B — Color encodes meaning in data visualization. Using multiple hues decoratively trains the viewer to ignore color — then it fails when you actually need it to communicate.

Q18: D — Bar #4 (0-indexed: index 3) → delay = 3 * 5 frames = 15 frames → 15/24fps ≈ 0.625 seconds.

Q19: B — Camera Z-position animation with a fixed map provides a natural zoom (perspective) effect. Scaling the map layer works but doesn't provide the same depth quality.

Q20: C — AEJuice Starter Pack includes transitions, text presets, and basic infographic elements — all motion graphics assets. Not footage, not audio, not 3D.

Q21: B — Staging: the viewer needs a clear focus at every moment. Multiple simultaneous changes with no hierarchy leave the eye without guidance — a staging failure.

Q22: B — Bar charts are superior for comparing values across multiple categories, especially when there are more than 5. Pie charts are for parts-of-a-whole with ≤5 categories.

Q23: B — A leading dot gives the eye something to follow — it "pulls" the line forward and makes the progressive reveal feel dynamic rather than passive.

Q24: A — After Effects cannot natively import CSV files as live data. Data-driven animation requires third-party scripts (e.g., Datamator, AEUX) or manual input via expressions.
```
